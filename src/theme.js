import { createMuiTheme } from '@material-ui/core'

export const them = createMuiTheme({
  palette: {
    primary: {
      main: '#2f2565'
    },
    secondary: {
      main: '#f00'
    }
  },
  typography: {
  }

})
